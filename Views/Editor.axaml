<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:quillborne.ViewModels"
    x:Class="quillborne.Views.Editor"
    x:DataType="vm:EditorViewModel"
>

    <Grid ColumnDefinitions="1*,2*,1*" Background="#FAFAFA">
        <ScrollViewer
            Grid.Column="1"
            HorizontalScrollBarVisibility="Hidden"
            VerticalScrollBarVisibility="Hidden"
            BringIntoViewOnFocusChange="False"
        >
            <Border
                Width="794"
                Height="1123"
                Background="#FAFAFA"
                HorizontalAlignment="Center"
                BoxShadow="0 4 12 0 #30000000"
                Margin="40"
            >
                <TextBox
                    Text="{Binding EditorText, Mode=OneWay}"
                    AcceptsReturn="true"
                    TextWrapping="Wrap"
                    AcceptsTab="true"
                    BorderThickness="0"
                    CaretBlinkInterval="1"
                    FontFamily="{StaticResource SourceSerif}"
                    FontSize="18"
                    Padding="75"
                    VerticalContentAlignment="Top"
                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                />
            </Border>
        </ScrollViewer>
    </Grid>

</UserControl>
